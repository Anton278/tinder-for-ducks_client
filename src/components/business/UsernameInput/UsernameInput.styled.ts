import styled from "styled-components";

export const UsernameInpWrapper = styled.div`
  position: relative;
`;

export const SpinnerWrapper = styled.div`
  position: absolute;
  top: 50%;
  right: 11px;
  transform: translateY(-50%);
`;

export const HelperError = styled.p`
  margin-top: 0.25rem;
  font-size: 0.875em;
  margin-bottom: 0;
`;
